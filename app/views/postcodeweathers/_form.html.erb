<%= form_for(@postcodeweather) do |f| %>
  <% if @postcodeweather.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@postcodeweather.errors.count, "error") %> prohibited this postcodeweather from being saved:</h2>

      <ul>
      <% @postcodeweather.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :postcode %><br>
    <%= f.text_field :postcode %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
