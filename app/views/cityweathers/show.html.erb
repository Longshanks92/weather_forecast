<p id="notice"><%= notice %></p>
<% require 'json' %>
<% request_uri = 'http://api.openweathermap.org/data/2.5/forecast/daily?q=' + @cityweather.city + "&APPID=17d7c464b1b3c40e4c48297d750a9010" %>
<% # couldn't get the data to be read, tried stack overflow with no avail, would have iterated through the hashes and formatted the data into type (weater, temperate etc) and days in tables for formatting %>
<% data = open(request_uri).read %>
<% weather = JSON.parsed(data) %>
<%= link_to 'Edit', edit_cityweather_path(@cityweather) %> |
<%= link_to 'Back', cityweathers_path %>
