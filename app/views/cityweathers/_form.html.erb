<%= form_for(@cityweather) do |f| %>
  <% if @cityweather.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cityweather.errors.count, "error") %> prohibited this cityweather from being saved:</h2>

      <ul>
      <% @cityweather.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
